[workspace]
resolver = "2"
members = ["engine", "protocol", "daemon", "cli", "uinputd", "vietcctl"]
exclude = ["ui", "vk"]

[workspace.dependencies]
vietc-engine = { path = "engine" }
vietc-protocol = { path = "protocol" }
